{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "gcc",
            "command": "gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${workspaceRoot}\\bin\\${fileBasenameNoExtension}.exe"
            ],
            "group": "build"
        }
    ]
}